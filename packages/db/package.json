{
  "name": "@linq/db",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate deploy",
    "lint": "eslint . --ext ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@prisma/client": "^5.9.1"
  },
  "devDependencies": {
    "@linq/config": "0.0.1",
    "prisma": "^5.9.1"
  }
}

import { Card, CardContent, CardHeader, CardTitle } from "@linq/ui";

export default function PeoplePage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>People</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Manage contacts and view relationship health here.</p>
      </CardContent>
    </Card>
  );
}
